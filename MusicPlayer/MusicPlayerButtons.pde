void musicPlayerButtons() {
  /*
  if () {//Play-Pause Button
    if (song1.isPlaying()) {
      song1.pause();
    } else if (song1.position() == song1.length()) {//.length() = length of song/end of song
      song1.rewind();
      song1.play();
    } else {
      song1.play(); //.play is the audio player function
    }
  } //End Play-Pause Button
  //
  if () {
    if (song1.isPlaying()) {//Stop Button
      song1.pause();
      song1.rewind();
    } else if (song1.position() == song1.length()) {//.length() = length of song/end of song
      song1.rewind();
    } else {
      song1.rewind();
    }
  }//End of Stop Button
  //
  if () song1.skip(1000); //End Fast Forward //you have to use milliseconds (1000 miliseconds = 1 second)
  if () song1.skip(-1000); //End Rewind 1 second
  //
  */
}
